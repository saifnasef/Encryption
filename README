This program is designed to AES encrypt and decrypt files

Usage:
args '-f' stands for file and it's followed by the filename you want to encrypt or decrypt
args '-e' stands for encrypt
args '-d' stands for decrypt
args '-p' is for password then it's followed by the password you want to use to encrypt or decrypt

examples:
python Enc&Dec.py -f Secret.txt -e -p SuperSecretKey
python Enc&Dec.py -f Secret.txt -d -p SuperSecretKey

required libraries:
pycrypto
